```trycmd
$ scryer-prolog -f --no-add-history input.pl -g test -g halt
test causes: error(existence_error(ffi_struct_constructor,.(culprit- $[u8;2]/1,.(suggestion- $[u8;2]/2,[]))),$ffi_allocate/4)

```